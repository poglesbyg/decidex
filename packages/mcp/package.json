{
  "name": "@decidex/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server — surfaces engineering decisions to Claude Code",
  "bin": {
    "decidex-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@decidex/core": "*",
    "@modelcontextprotocol/sdk": "^1.10.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
